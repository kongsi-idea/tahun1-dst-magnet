# handoff — tahun1-dst-magnet（磁力创造实验室）

> 最后更新：2026-09-17（Claude Sonnet 5 @ 这台 Mac）

## 现在的状态
- v2「磁力创造实验室」已完整发布：四个工作区（磁力探索桌／磁极谜题场／公平实验室／挑战创造工坊）、课堂投影／家庭探索双模式、老师控制台，均已通过 1440px 与 375px 真实浏览器交互验收（`verify` 子代理，Playwright）。
- 发布前验收发现一个小瑕疵并已修复：点击暂停遮罩本身恢复全班时，老师控制台里「暂停全班」按钮文字没有跟着变回来（会误导老师再次点击把已恢复的班级重新暂停）。已在 `app.js` 第 54 行同步重置按钮文字，commit `a777f82`。
- 历史坑「课堂暂停遮罩卡住首页」（CSS `display:grid` 盖掉 `[hidden]`）复查确认修复仍生效，两个视口下均未回归。
- **已 push 并部署生产**：GitHub `main` 已推（含本次修复），Vercel 生产部署完成，正式 alias `https://tahun1-dst-magnet.vercel.app` 已复核（本次部署自动别名就指对了，不像 Hub 那样需要手动 `alias set`）。
- 线上复验：curl 确认标题「磁力创造实验室」、四个 `data-tool` 工作区、暂停按钮修复均已生效；Playwright 浏览器复验标题、四工作区入口、`app.js`/`styles.css` 200、console 无报错。
- Kongsi Idea Hub 一侧：已用 Playwright 实际操作正式网址拍了 4 张 v2 真实截图（首页/磁极谜题场/挑战创造工坊/老师控制台），替换进 `app.js` 的 `tahun1-dst-magnet.thumbnails`，旧版 4 张 v1 截图已搬到 `~/Documents/待删除/kongsi-idea-thumbs-tahun1-dst-magnet-v1-20260917/`（未删除）。Hub 详情页与「开始使用」外链已浏览器复验通过。

## 已定产品要求（不变，供后续参考）
- 约 7 岁；单次 15 分钟；简体中文短句，不需要语音。
- 课堂老师投影、家庭学生一人一机；需适配电脑、手机和平板。
- 学习覆盖 DSKP 7.1.1–7.1.6 与 TP6，但拆成多次短活动。
- 学生偏好自由探索与创造；失败后给注意／关系／操作三层线索。
- 老师工具包括暂停、隐藏结果、磁力线、预测投票、讨论题、保存结果与打印实验单。
- 教师须知必须说明二维模拟需要成人支架与真实活动连接，并标明模拟并非精密测量。

## 下一步 / 未完成限制
- 老师控制台的「预测投票」「清除票数」「打印实体实验单」「保存本次结果」、工作区「重置」按钮，本轮未逐一点击验证（已验证的是暂停/隐藏结果/磁力线/讨论问题四项核心控制）。
- 磁极谜题场／公平实验室在**家庭模式**下的真实拖动交互，只在探索桌和创造工坊两个工作区做了手机视口下的完整验证；家庭模式下这两个工作区只验证了「能打开、无报错、能返回 Hub」。画布物理不因模式分支，可合理推断可用，但非直接观测证据。
- 未做视觉审美评价（配色、间距等），只验证功能与交互正确性。
- 老师课堂实测反馈尚未收集（新工具惯例，等真实课堂使用后再回填满意度）。

## 权限备注
- 本次生产部署（`vercel --prod --yes`）被 `~/Documents/my-agent/.ops/guard-policy.json` 的 `deploy_allowlist` 挡下（`tahun1-dst-magnet` 不在名单内，只有 hks-hub/kongsi-idea/3g-assessment/fgs-checkin-system/kk2-selamat/class_system-1/bliayad-2026 七个专案可以）——不是深夜限制。老师本人在对话里用 `!` 前缀直接跑了部署命令，Claude 未绕过 guard。

## Git 同步
- 本地：`main` 与 `origin/main` 一致（`a777f82`），工作区干净。
- GitHub：✅ 已推。
- 部署：✅ 已部署生产，alias `https://tahun1-dst-magnet.vercel.app` 已复核，线上内容与本地一致（curl + Playwright 双重复验）。
